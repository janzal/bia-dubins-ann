Call ./install.sh to build imr-h.lite, imr-h.gui, and opendubins.

imr-h.lite and imr-h.gui depends on CAIRO, LOG4CXX, and BOOST
In a case the compilation fail, adjust paths to the libraries in Mk/libs.mk


After successfull compilation particular header files and libraries are
copied into ../include and ../lib directories.
